/**
 * Unified UI class strings — import with `cn()` for consistent product chrome.
 * One system for public marketing, fleet, and admin (admin uses `.admin-app-root` token overrides).
 */

/** Page / section vertical rhythm */
export const pageSection = 'py-16 sm:py-20 lg:py-24'
export const pageSectionTight = 'py-12 sm:py-16'

/** Standard content width inside sections */
export const contentStack = 'flex min-w-0 flex-col gap-8 sm:gap-10'

/** 12-column product grid */
export const grid12 = 'grid min-w-0 grid-cols-1 gap-8 sm:gap-10 lg:grid-cols-12 lg:gap-x-8'

export const labelCaps = 'text-xs font-medium uppercase tracking-wider text-muted'

export const textH1 =
  'text-[1.75rem] font-semibold leading-tight tracking-tight text-soft sm:text-4xl lg:text-[2.5rem]'

export const textH2 = 'text-xl font-semibold tracking-tight text-soft sm:text-2xl'

export const textLead = 'text-base leading-relaxed text-muted sm:text-[1.0625rem] sm:leading-relaxed'

export const textBody = 'text-sm leading-relaxed text-muted'

export const textMeta = 'text-xs text-muted'

/** Primary surface — cards, panels, search bar */
export const surface =
  'rounded-xl border border-stroke bg-carbon'

/** Hairline dividers */
export const divider = 'border-t border-stroke'
